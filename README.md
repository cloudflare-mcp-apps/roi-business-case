# roi-business-case

> Interactive ROI Business Case Builder with dynamic sliders and charts for B2B sales

**Slug:** `roi-business-case` · **SDK:** `^1.29.0` · **ext-apps:** `^1.7.0` · **Protocol:** `2025-11-25`

## Tools (1)

- `build_business_case`

## Resources (1)

- `widgetResource` _(name resolved at runtime — see `src/server.ts` or `src/mcp-handler.ts`)_

## Prompts (2)

- `business-case`
- `roi-quick`

## Widgets (1)

- `web/widgets/widget.html`

## Cloudflare Bindings

| Type | Binding | Detail |
|------|---------|--------|
| ASSETS | `ASSETS` | ./web/dist/widgets |
| D1 | `DB` | mcp-oauth |

## Auth

JWT via WorkOS AuthKit JWKS + API key (`wtyk_` prefix) D1 hash lookup. Cloudflare canonical `createMcpHandler` pattern (`src/server.ts` factory) for new servers; legacy thin resource server pattern (`src/mcp-handler.ts` LRU cache) for older projects. Zero per-server secrets — auth centralized at `panel.wtyczki.ai`. See `new_oauth/system.md`.

## Deploy

Triggered by `git push` (Cloudflare Workers Builds). Test in [Cloudflare AI Playground](https://playground.ai.cloudflare.com/).

```bash
# from repo root
npx tsc --noEmit && npx wrangler deploy --dry-run --cwd projects/roi-business-case
```

---

<!-- Auto-generated by `npm run readme:sync -- roi-business-case`. Do not edit manually — changes are overwritten on next sync. -->
